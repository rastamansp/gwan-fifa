# ============ Backend (apps/api) ============
PORT=3015
API_PREFIX=api

# Claude (visao) — chave SO no backend, nunca no client
ANTHROPIC_API_KEY=sk-ant-xxxxx
CLAUDE_MODEL=claude-opus-4-8

# MinIO (S3) — credenciais so no backend
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=stickers
SIGNED_URL_TTL=3600

# App
PUBLIC_URL=http://localhost:5188
GWAN_SITE_URL=https://gwan.cloud
CORS_ORIGIN=http://localhost:5188

# ============ Frontend (apps/web) ============
VITE_API_BASE_URL=http://localhost:3015
WEB_PORT=5188
